// Standalone layout — no Sidebar, no top nav.
// The select-airport page is a full-screen picker shown before entering the manager portal.
export default function SelectAirportLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
